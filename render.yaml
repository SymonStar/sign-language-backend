services:
  - type: web
    name: sign-language-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_v2:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
